<%= link_to 'New Article', new_article_path, { class: 'text-button' } %>

<div class="article-cards">
  <% @articles.each do |article| %>
    <%= render 'card', article: article %>
  <% end %>
</div>
